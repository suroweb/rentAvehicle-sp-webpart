---
phase: 08-ux-polish-availability-strip-navigation-and-booking-process-refinement
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.tsx
  - spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.module.scss
  - spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.tsx
  - spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.module.scss
  - spfx/src/webparts/rentaVehicle/components/VehicleDetail/BookingForm.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Desktop VehicleDetail page shows side-by-side layout: availability on left, booking form sticky on right"
    - "Vehicle photo is a compact inline thumbnail (80px wide) next to Make/Model/Year and specs, not a 300px hero"
    - "Booking form panel is always visible on desktop (not hidden after bookingSuccess)"
    - "Day View (AvailabilityTimeline) has left/right arrow buttons for day-by-day navigation alongside its DatePicker"
    - "DatePickers set minDate = today across all components"
    - "Hour dropdowns hide past hours when today is selected"
    - "Strip and timeline gray out past slots as non-clickable"
    - "Default form values: start date = today, start hour = next full hour, end = start + 1 hour"
    - "Visual warning (MessageBar) when selected time overlaps a booked slot on the strip"
    - "Green success MessageBar appears at top after booking, form resets below it"
  artifacts:
    - path: "spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.tsx"
      provides: "Side-by-side desktop layout with compact header and overlap warning"
      contains: "vehicleDetailLayout"
    - path: "spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.module.scss"
      provides: "Flexbox two-column layout, compact header styles, sticky right column"
      contains: "leftColumn"
    - path: "spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.tsx"
      provides: "Day-by-day arrow navigation alongside DatePicker"
      contains: "handlePrevDay"
    - path: "spfx/src/webparts/rentaVehicle/components/VehicleDetail/BookingForm.tsx"
      provides: "Past-hour filtering in dropdowns, smart defaults"
      contains: "getFilteredHourOptions"
  key_links:
    - from: "VehicleDetail.tsx"
      to: "VehicleDetail.module.scss"
      via: "CSS class references for two-column layout"
      pattern: "vehicleDetailLayout|leftColumn|rightColumn"
    - from: "VehicleDetail.tsx"
      to: "BookingForm.tsx"
      via: "overlapWarning state passed as prop or rendered in parent"
      pattern: "overlapWarning"
    - from: "AvailabilityTimeline.tsx"
      to: "AvailabilityTimeline.module.scss"
      via: "Navigation arrow styles"
      pattern: "navButton|handlePrevDay|handleNextDay"
---

<objective>
Restructure the VehicleDetail page to a side-by-side desktop layout (availability left, sticky booking form right), replace the 300px hero image with a compact inline thumbnail, add day-by-day arrow navigation to the Day View timeline, implement comprehensive past-time prevention (minDate on DatePickers, filtered hour dropdowns, grayed-out past slots), add an overlap warning when the form selection conflicts with a booked strip slot, and ensure the form is always visible with smart defaults.

Purpose: Desktop layout overhaul and interaction refinements that make the booking process faster and less error-prone. Builds on Plan 01's strip navigation and bidirectional sync.
Output: Professional side-by-side desktop layout with sticky form, navigable Day View, and comprehensive past-time guards.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ux-polish-availability-strip-navigation-and-booking-process-refinement/08-CONTEXT.md
@.planning/phases/08-ux-polish-availability-strip-navigation-and-booking-process-refinement/08-RESEARCH.md
@.planning/phases/08-ux-polish-availability-strip-navigation-and-booking-process-refinement/08-01-SUMMARY.md
@spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.tsx
@spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.module.scss
@spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.tsx
@spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.module.scss
@spfx/src/webparts/rentaVehicle/components/VehicleDetail/BookingForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop side-by-side layout, compact vehicle header, Day View arrow navigation, and overlap warning</name>
  <files>
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.tsx
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/VehicleDetail.module.scss
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.tsx
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.module.scss
  </files>
  <action>
**VehicleDetail.tsx — Layout restructure:**

1. **Remove the `max-width: 900px` constraint** from the top-level `.vehicleDetail` and introduce a new two-column flexbox layout wrapper.

2. **Replace the hero image section** with a compact inline header:
```jsx
<div className={styles.compactHeader}>
  {vehicle.photoUrl ? (
    <img src={vehicle.photoUrl} alt={vehicleName} className={styles.compactThumbnail} />
  ) : (
    <div className={styles.compactThumbnailPlaceholder}>
      <Icon iconName="Car" className={styles.compactPlaceholderIcon} />
    </div>
  )}
  <div className={styles.compactInfo}>
    <h2 className={styles.compactTitle}>{vehicleName}</h2>
    <div className={styles.compactSpecs}>
      <span>{vehicle.licensePlate}</span>
      <span>{vehicle.categoryName}</span>
      <span><Icon iconName="People" /> {vehicle.capacity} seats</span>
      <span>{vehicle.locationName}</span>
    </div>
  </div>
</div>
```

3. **Two-column layout structure:**
```jsx
<div className={styles.vehicleDetailLayout}>
  <div className={styles.leftColumn}>
    {/* backNav */}
    {/* compactHeader */}
    {/* successBar (conditional) */}
    {/* overlapWarning (conditional) */}
    {/* Pivot with AvailabilityStrip and AvailabilityTimeline */}
  </div>
  <div className={styles.rightColumn}>
    {/* BookingForm — ALWAYS visible, even after bookingSuccess */}
    <BookingForm ... />
  </div>
</div>
```

4. **Always show BookingForm** — Remove the `{!bookingSuccess && ...}` conditional wrapper. The form should always render on desktop. After success, show the green MessageBar at the top of the left column and reset the form (Plan 01 already resets via prefillDate/prefillStartHour).

5. **Overlap warning** — Add a computed `overlapWarning` boolean using `React.useMemo` that compares the form's current `startTimeUtc`/`endTimeUtc` against `availabilitySlots`. When true, render a `MessageBar` with `MessageBarType.warning` in the left column saying "This slot appears booked. You can still submit — the server will verify availability." This requires knowing the form's current times. Add `formStartTimeUtc` and `formEndTimeUtc` as state in VehicleDetail, updated via a new `onFormTimesChange` callback from BookingForm.

Actually, simpler approach: pass `availabilitySlots` to BookingForm and let it compute the overlap internally and show the warning above its own submit button. Add an `availabilitySlots` prop to BookingForm.

6. **Remove the old specsSection/specsGrid** — All that spec information is now in the compact header inline. Remove the `<div className={styles.specsSection}>...` block entirely.

**VehicleDetail.module.scss — Layout overhaul:**

1. **Remove `.vehicleDetail` max-width** (or set to `none`).

2. **Add two-column flexbox layout:**
```scss
.vehicleDetailLayout {
  display: flex;
  flex-direction: row;
  gap: 24px;
  align-items: flex-start;
}

.leftColumn {
  flex: 1 1 0;
  min-width: 0;
}

.rightColumn {
  flex: 0 0 360px;
  position: sticky;
  top: 24px;
  max-height: calc(100vh - 48px);
  overflow-y: auto;
}
```

3. **Add compact header styles:**
```scss
.compactHeader {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 16px;
}

.compactThumbnail {
  width: 80px;
  height: 60px;
  border-radius: 6px;
  object-fit: cover;
  flex: 0 0 80px;
}

.compactThumbnailPlaceholder {
  width: 80px;
  height: 60px;
  border-radius: 6px;
  background: #f3f2f1;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 80px;
}

.compactPlaceholderIcon {
  font-size: 28px;
  color: #a19f9d;
}

.compactInfo {
  flex: 1 1 auto;
}

.compactTitle {
  font-size: 20px;
  font-weight: 600;
  color: $color-text-primary;
  margin: 0 0 4px 0;
}

.compactSpecs {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  font-size: 13px;
  color: $color-text-secondary;
}
```

4. **Add overlap warning style:**
```scss
.overlapWarning {
  margin-bottom: 12px;
}
```

5. **Remove old hero image styles** (`.heroImage`, `.heroImg`, `.heroPlaceholder`, `.heroPlaceholderIcon`) — they are no longer used. Also remove `.vehicleTitle` (replaced by `.compactTitle`), `.specsSection`, `.specsGrid`, `.specItem`, `.specLabel`, `.specValue`, `.specMono`, `.specBadge`, `.specIcon`.

6. **Update mobile media query** — On mobile (`max-width: $mobile-breakpoint`), the layout should stack:
```scss
@media (max-width: $mobile-breakpoint) {
  .vehicleDetailLayout {
    flex-direction: column;
  }

  .rightColumn {
    display: none; /* Hidden on mobile — form will be in bottom sheet (Plan 03) */
  }
}
```

**AvailabilityTimeline.tsx — Day-by-day arrow navigation:**

1. Add left/right arrow `IconButton`s (from Fluent UI) alongside the existing DatePicker in the timeline header.
2. Left arrow: decrements selectedDate by 1 day. Disabled when selectedDate equals today (cannot go to past).
3. Right arrow: increments selectedDate by 1 day. No upper bound needed.
4. Import `IconButton` from `@fluentui/react/lib/Button`.
5. Add handlers:
```typescript
const handlePrevDay = React.useCallback(function onPrevDay(): void {
  const prev = new Date(selectedDate.getFullYear(), selectedDate.getMonth(), selectedDate.getDate() - 1);
  const today = getToday();
  if (prev >= today) {
    setSelectedDate(prev);
  }
}, [selectedDate]);

const handleNextDay = React.useCallback(function onNextDay(): void {
  setSelectedDate(new Date(selectedDate.getFullYear(), selectedDate.getMonth(), selectedDate.getDate() + 1));
}, [selectedDate]);
```
6. Render arrows in the header section:
```typescript
React.createElement(IconButton, {
  iconProps: { iconName: 'ChevronLeft' },
  title: 'Previous day',
  ariaLabel: 'Previous day',
  onClick: handlePrevDay,
  disabled: selectedDate.getTime() <= getToday().getTime(),
}),
React.createElement(DatePicker, datePickerProps),
React.createElement(IconButton, {
  iconProps: { iconName: 'ChevronRight' },
  title: 'Next day',
  ariaLabel: 'Next day',
  onClick: handleNextDay,
}),
```

**AvailabilityTimeline.module.scss — Nav button styling:**

Add a `.timelineNav` class for the arrow/datepicker group:
```scss
.timelineNav {
  display: flex;
  align-items: flex-end;
  gap: 4px;
}
```
  </action>
  <verify>
    <automated>cd "/Users/dancomilosevici/DevLeet/Microsoft 365 Solutions/Rentavehicle/spfx" && npx heft build --clean 2>&1 | tail -20</automated>
    <manual>Load VehicleDetail on desktop: verify side-by-side layout with compact header on left, sticky form on right. Toggle to Day View, verify left/right arrows navigate days. Check that form is always visible even after booking success.</manual>
  </verify>
  <done>Desktop shows two-column layout with compact vehicle header, sticky booking form, and overlap warning. Day View has day-by-day arrow navigation. Old hero image and spec grid are replaced with compact inline display.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive past-time prevention and smart form defaults</name>
  <files>
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/BookingForm.tsx
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityStrip.tsx
    spfx/src/webparts/rentaVehicle/components/VehicleDetail/AvailabilityTimeline.tsx
  </files>
  <action>
**BookingForm.tsx — Past-hour filtering and smart defaults:**

1. **Add a `getFilteredHourOptions` function** that filters out past hours when today is selected:
```typescript
function getFilteredHourOptions(selectedDate: Date): IDropdownOption[] {
  const now = new Date();
  const isToday = selectedDate.getFullYear() === now.getFullYear()
    && selectedDate.getMonth() === now.getMonth()
    && selectedDate.getDate() === now.getDate();

  if (!isToday) return HOUR_OPTIONS;

  const currentHour = now.getHours();
  const filtered: IDropdownOption[] = [];
  for (let i = 0; i < HOUR_OPTIONS.length; i++) {
    if ((HOUR_OPTIONS[i].key as number) > currentHour) {
      filtered.push(HOUR_OPTIONS[i]);
    }
  }
  return filtered;
}
```

2. **Use `getFilteredHourOptions`** for the start hour Dropdown options — pass `getFilteredHourOptions(startDate)` as the `options` prop. For end hour Dropdown: use `getFilteredHourOptions(endDate)`.

3. **Smart defaults are already correct** — `getToday()` for start date and `getNextFullHour()` for start hour. Verify that `getNextFullHour` correctly returns the next full hour and that the initial endHour is start+1. These are already implemented correctly.

4. **Add the overlap warning** — Add `availabilitySlots?: IVehicleAvailabilitySlot[]` to `IBookingFormProps`. Compute overlap:
```typescript
const overlapWarning = React.useMemo(function checkOverlap(): boolean {
  if (!availabilitySlots || availabilitySlots.length === 0) return false;
  const formStartMs = new Date(startTimeUtc).getTime();
  const formEndMs = new Date(endTimeUtc).getTime();
  if (isNaN(formStartMs) || isNaN(formEndMs) || formEndMs <= formStartMs) return false;
  for (let i = 0; i < availabilitySlots.length; i++) {
    const slotStartMs = new Date(availabilitySlots[i].startTime).getTime();
    const slotEndMs = new Date(availabilitySlots[i].endTime).getTime();
    if (formStartMs < slotEndMs && formEndMs > slotStartMs) return true;
  }
  return false;
}, [availabilitySlots, startTimeUtc, endTimeUtc]);
```
Render a `MessageBar` with `MessageBarType.warning` above the form actions when `overlapWarning` is true: "This slot appears booked. You can still submit — the server will verify availability."

5. **Pass availabilitySlots to BookingForm** from VehicleDetail.tsx (update the BookingForm render in VehicleDetail to add `availabilitySlots={availabilitySlots}`).

**AvailabilityStrip.tsx — Gray out past slots:**

In the dayColumns build loop, when generating hour blocks:
- Compare `dayDate` against today. If the day is today, compare the hour against `new Date().getHours()`.
- If the day is in the past (should not happen with week navigation starting from today) or if the hour is past on today, mark the slot with a new `isPast` flag.
- Add a new CSS class `.stripBlockPast` for past slots: grayed out, non-clickable, no hover.
- Past slots should not trigger `onSlotClick`.

```typescript
const nowHour = new Date().getHours();
const isPast = isToday && h <= nowHour;
```

When rendering:
- If `isPast`: use `styles.stripBlockPast` class, no onClick
- If `isBooked`: use `styles.stripBlockBooked` class, no onClick
- If free and not past: use `styles.stripBlockFree` class, with onClick

Add to VehicleDetail.module.scss:
```scss
.stripBlockPast {
  height: 16px;
  background: #f3f2f1;
  border-radius: 2px;
  cursor: default;
  opacity: 0.5;
}
```

**AvailabilityTimeline.tsx — Gray out past slots:**

In the free slot rendering loop, check if the day is today and the hour has passed. If so, apply a `.pastSlot` class instead of `.freeSlot`. The pastSlot should be grayed out and non-clickable (no onClick handler).

Add to AvailabilityTimeline.module.scss:
```scss
.pastSlot {
  border-right: 1px solid #f3f2f1;
  border-bottom: 1px solid #f3f2f1;
  cursor: default;
  background: #f3f2f1;
  opacity: 0.5;
}
```
  </action>
  <verify>
    <automated>cd "/Users/dancomilosevici/DevLeet/Microsoft 365 Solutions/Rentavehicle/spfx" && npx heft build --clean 2>&1 | tail -20</automated>
    <manual>Verify: (1) When today is selected in the form, only future hours appear in dropdowns. (2) Past hours on today's strip column are grayed out. (3) Past slots on Day View are grayed out. (4) Select a time that overlaps a booked slot and see the warning MessageBar. (5) Submit still works despite warning.</manual>
  </verify>
  <done>Past hours are hidden from dropdowns when today is selected. Past slots on strip and timeline are grayed out and non-clickable. Overlap warning appears when selected times conflict with booked slots. Smart defaults set start to next full hour today.</done>
</task>

</tasks>

<verification>
1. SPFx builds: `cd spfx && npx heft build --clean`
2. Desktop layout: side-by-side with sticky right column
3. Compact header: 80px thumbnail, inline specs, no hero image
4. Day View arrows: navigate days, left arrow disabled on today
5. Past hours hidden from dropdowns when today selected
6. Past slots grayed out on strip and timeline
7. Overlap warning displays when form times conflict with booked slots
8. Form always visible (not hidden after success)
9. Success MessageBar shows at top of left column
</verification>

<success_criteria>
- VehicleDetail renders in two-column side-by-side layout on desktop
- Vehicle photo is 80px thumbnail, not 300px hero
- BookingForm is sticky on the right and always visible
- Day View has functional day-by-day arrow navigation
- All DatePickers have minDate = today
- Hour dropdowns filter past hours when today is selected
- Past slots are grayed out and non-clickable on both strip and timeline
- Overlap warning MessageBar appears when form selection conflicts with booked slot
- SPFx project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-ux-polish-availability-strip-navigation-and-booking-process-refinement/08-02-SUMMARY.md`
</output>
