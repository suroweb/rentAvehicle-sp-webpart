---
phase: 09-live-tenant-verification
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - spfx/config/package-solution.json
  - spfx/teams/manifest.json
  - .planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md
autonomous: false
gap_closure: true
requirements: [VRFY-04, VRFY-05]
must_haves:
  truths:
    - "SPFx Teams app manifest declares activityTypes: bookingConfirmed, managerBookingAlert, pickupReminder, returnReminder, bookingOverdue"
    - "Teams app is installed for the target test user"
    - "Teams activity feed notification appears for the booking employee after creating a booking"
    - "Teams activity feed notification appears for the manager after creating a booking"
  artifacts:
    - path: "spfx/teams/manifest.json"
      provides: "Teams app manifest with webApplicationInfo and activities sections"
      contains: "activityTypes, bookingConfirmed, managerBookingAlert"
    - path: "spfx/config/package-solution.json"
      provides: "SPFx package-solution with Teams app manifest reference"
    - path: ".planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md"
      provides: "VRFY-04 and VRFY-05 marked as full pass (not partial)"
  key_links:
    - from: "spfx/teams/manifest.json"
      to: "api/src/services/notificationService.ts"
      via: "activityTypes in manifest must match activityType values in sendTeamsActivityNotification calls"
      pattern: "bookingConfirmed|managerBookingAlert|pickupReminder|returnReminder|bookingOverdue"
    - from: "spfx/config/package-solution.json"
      to: "spfx/teams/manifest.json"
      via: "SPFx build bundles teams/manifest.json into the .sppkg"
---

<objective>
Create a Teams app manifest declaring the required activity types for the RentAVehicle SPFx webpart, deploy the app to Teams for the test user, and re-verify that Teams activity feed notifications deliver end-to-end for both the employee and manager.

Purpose: The 403 Forbidden error on Teams activity notifications is not a code defect -- it is a deployment prerequisite. The Graph API `/users/{userId}/teamwork/sendActivityNotification` endpoint requires the calling app to be installed in Teams for the target user with matching activityTypes declared in the manifest. This plan closes Gap 1 from the verification report.

Output: Teams app manifest with activityTypes, app deployed and installed for test user, VRFY-04 and VRFY-05 verified as full pass.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-live-tenant-verification/09-CONTEXT.md
@.planning/phases/09-live-tenant-verification/09-VERIFICATION.md

Key source files:
@spfx/config/package-solution.json
@spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.manifest.json
@api/src/services/notificationService.ts

Prior plan context:
@.planning/phases/09-live-tenant-verification/09-03-SUMMARY.md

<interfaces>
From api/src/services/notificationService.ts -- activityType values used in code:
```typescript
// Employee booking confirmation
sendTeamsActivityNotification(userId, 'bookingConfirmed', previewText, bookingId)

// Manager booking alert
sendTeamsActivityNotification(managerId, 'managerBookingAlert', previewText, bookingId)

// Pickup reminder (from scheduled function)
sendTeamsActivityNotification(userId, 'pickupReminder', previewText, bookingId)

// Return reminder (from scheduled function)
sendTeamsActivityNotification(userId, 'returnReminder', previewText, bookingId)

// Overdue alert (from scheduled function)
sendTeamsActivityNotification(userId, 'bookingOverdue', previewText, bookingId)
```

From spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.manifest.json:
- webpart ID: "c79feb6b-b2b6-4102-bbc4-a2b3a9beff18"
- alias: "RentaVehicleWebPart"
- supportedHosts: ["SharePointWebPart", "TeamsPersonalApp", "TeamsTab", "SharePointFullPage"]

From spfx/config/package-solution.json:
- solution ID: "faa3486e-fc56-40d2-b420-c5b9d30257b3"
- SPFx package name: "renta-vehicle-client-side-solution"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Teams app manifest with activityTypes and update package-solution.json</name>
  <files>
    spfx/teams/manifest.json
    spfx/config/package-solution.json
  </files>
  <action>
Create a Teams app manifest at `spfx/teams/manifest.json` that declares the activity types used by the notification service. This is required by the Graph API for `/users/{userId}/teamwork/sendActivityNotification` to succeed.

**Create `spfx/teams/manifest.json`** with the following structure:

```json
{
  "$schema": "https://developer.microsoft.com/en-us/json-schemas/teams/v1.17/MicrosoftTeams.schema.json",
  "manifestVersion": "1.17",
  "version": "1.0.0",
  "id": "faa3486e-fc56-40d2-b420-c5b9d30257b3",
  "developer": {
    "name": "RentAVehicle",
    "websiteUrl": "https://contoso.sharepoint.com",
    "privacyUrl": "https://contoso.sharepoint.com",
    "termsOfUseUrl": "https://contoso.sharepoint.com"
  },
  "name": {
    "short": "RentAVehicle",
    "full": "RentAVehicle - Internal Vehicle Rental"
  },
  "description": {
    "short": "Book fleet vehicles at your location",
    "full": "Internal vehicle rental system. Employees can find and book available vehicles at their office location. Integrates with Outlook calendars and sends Teams notifications for booking confirmations, reminders, and alerts."
  },
  "icons": {
    "color": "c79feb6b-b2b6-4102-bbc4-a2b3a9beff18_color.png",
    "outline": "c79feb6b-b2b6-4102-bbc4-a2b3a9beff18_outline.png"
  },
  "accentColor": "#0078D4",
  "staticTabs": [
    {
      "entityId": "rentavehicle",
      "name": "RentAVehicle",
      "contentUrl": "https://{teamSiteDomain}/_layouts/15/TeamsLogon.aspx?SPFX=true&dest=/_layouts/15/teamshostedapp.aspx%3Fteams%26personal%26componentId=c79feb6b-b2b6-4102-bbc4-a2b3a9beff18%26forceLocale={locale}",
      "scopes": ["personal"]
    }
  ],
  "permissions": ["identity", "messageTeamMembers"],
  "validDomains": [
    "*.sharepoint.com",
    "*.microsoft.com"
  ],
  "webApplicationInfo": {
    "id": "<<AZURE_CLIENT_ID>>",
    "resource": "api://<<AZURE_CLIENT_ID>>"
  },
  "activities": {
    "activityTypes": [
      {
        "type": "bookingConfirmed",
        "description": "A vehicle booking has been confirmed",
        "templateText": "{actor} confirmed booking for {vehicle}"
      },
      {
        "type": "managerBookingAlert",
        "description": "An employee created a new vehicle booking",
        "templateText": "{actor} booked {vehicle}"
      },
      {
        "type": "pickupReminder",
        "description": "Reminder to pick up a booked vehicle",
        "templateText": "Pickup reminder for {vehicle}"
      },
      {
        "type": "returnReminder",
        "description": "Reminder to return a booked vehicle",
        "templateText": "Return reminder for {vehicle}"
      },
      {
        "type": "bookingOverdue",
        "description": "A vehicle booking is overdue for return",
        "templateText": "{vehicle} return is overdue"
      }
    ]
  }
}
```

**IMPORTANT:** Replace `<<AZURE_CLIENT_ID>>` with the actual Entra ID app registration client ID from `api/local.settings.json` (`AZURE_CLIENT_ID` value: `<your-client-id>`). The `webApplicationInfo.id` and `webApplicationInfo.resource` MUST match the Entra ID app registration that the Azure Functions API uses for Graph API calls.

**The five activityTypes MUST exactly match** the string values passed to `sendTeamsActivityNotification()` in `api/src/services/notificationService.ts`:
1. `bookingConfirmed` -- sent to employee on booking creation
2. `managerBookingAlert` -- sent to manager on booking creation
3. `pickupReminder` -- sent before pickup time
4. `returnReminder` -- sent before return time
5. `bookingOverdue` -- sent when booking is overdue

**Update `spfx/config/package-solution.json`** -- no changes needed. SPFx build automatically bundles the `teams/` folder contents into the `.sppkg` package. The existing `teams/` folder already has the icon files (`c79feb6b-b2b6-4102-bbc4-a2b3a9beff18_color.png` and `c79feb6b-b2b6-4102-bbc4-a2b3a9beff18_outline.png`). Verify the manifest icons reference these filenames correctly.
  </action>
  <verify>
    <automated>test -f spfx/teams/manifest.json && grep -c "activityTypes\|bookingConfirmed\|managerBookingAlert\|pickupReminder\|returnReminder\|bookingOverdue" spfx/teams/manifest.json</automated>
  </verify>
  <done>Teams app manifest exists at spfx/teams/manifest.json with all 5 activityTypes matching the notificationService.ts values, webApplicationInfo pointing to the correct Entra ID app registration, and icon references matching existing files in the teams/ folder.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy SPFx package to Teams and install for test user</name>
  <files>.planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md</files>
  <action>
User must deploy the SPFx package (with the new Teams manifest) to the SharePoint App Catalog and install it as a Teams app for the test user. This cannot be automated -- requires SharePoint admin and Teams admin access.
  </action>
  <what-built>
Claude created the Teams app manifest at `spfx/teams/manifest.json` with all 5 activityTypes that the notification service sends. The manifest includes `webApplicationInfo` pointing to the Entra ID app registration and `activities.activityTypes` declarations for bookingConfirmed, managerBookingAlert, pickupReminder, returnReminder, and bookingOverdue.

Now the SPFx package needs to be rebuilt and deployed to make the Teams app available.
  </what-built>
  <how-to-verify>
**Step 1: Build the SPFx package**
```bash
cd spfx && npm run build && npx gulp bundle --ship && npx gulp package-solution --ship
```
This produces `spfx/sharepoint/solution/renta-vehicle.sppkg`.

**Step 2: Upload to SharePoint App Catalog**
1. Go to your SharePoint tenant App Catalog (e.g., `https://contoso.sharepoint.com/sites/appcatalog/_layouts/15/tenantAppCatalog.aspx/manageApps`)
2. Upload (or replace) `renta-vehicle.sppkg`
3. When prompted, check "Make this solution available to all sites in the organization" and deploy
4. In the API access page, approve any pending permission requests if prompted

**Step 3: Sync to Teams**
1. In the App Catalog, select the "renta-vehicle-client-side-solution" app
2. Click "Sync to Teams" in the ribbon (or it may happen automatically)
3. Wait 1-2 minutes for sync to complete

**Step 4: Install for test user**
1. Open Microsoft Teams
2. Go to Apps (left sidebar) -> Built for your org (or "Manage your apps")
3. Find "RentAVehicle" and click "Add" to install it as a personal app
4. Alternatively, an admin can install it for specific users via Teams Admin Center:
   - Go to `https://admin.teams.microsoft.com/policies/manage-apps`
   - Find "RentAVehicle" -> assign to test user

**Step 5: Verify installation**
1. In Teams, click "..." (more apps) in the left sidebar
2. Confirm "RentAVehicle" appears in your installed apps
3. Clicking it should open the SPFx webpart inside Teams

Report back:
- SPFx package built successfully? (yes/no)
- Uploaded to App Catalog? (yes/no)
- Synced to Teams? (yes/no)
- App visible in Teams? (yes/no)
- Any errors encountered?
  </how-to-verify>
  <resume-signal>
Report deployment status:
1. SPFx package built: yes/no
2. Uploaded to App Catalog: yes/no
3. Synced to Teams: yes/no
4. App installed for test user: yes/no
5. Errors: none / describe

Or describe issues for troubleshooting.
  </resume-signal>
  <verify>
    <automated>test -f spfx/teams/manifest.json && echo "PASS: manifest exists"</automated>
  </verify>
  <done>SPFx package with Teams manifest deployed to App Catalog, synced to Teams, and installed for the test user. The app appears in Teams with the correct name and icon.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-verify Teams activity notifications end-to-end (VRFY-04 + VRFY-05)</name>
  <files>.planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md</files>
  <action>
After the Teams app is installed, create a booking and verify that Teams activity feed notifications now deliver successfully for both the employee and manager. Update the verification checklist to mark VRFY-04 and VRFY-05 as full pass.
  </action>
  <what-built>
The Teams app is now installed with the manifest declaring all 5 activityTypes. The notification service code was already correct (verified in 09-03). The only missing piece was the Teams app deployment, which is now complete.

This task re-runs the VRFY-04 and VRFY-05 verification that previously got 403 Forbidden.
  </what-built>
  <how-to-verify>
**Prerequisites:** API must be running (`cd api && npm start`). Teams app must be installed for the test user (completed in Task 2).

**Step 1: Create a new booking**
```bash
curl -X POST http://localhost:7071/api/bookings \
  -H "Content-Type: application/json" \
  -d '{
    "vehicleId": <VEHICLE_ID>,
    "startTime": "<FUTURE_DATE_ISO>",
    "endTime": "<FUTURE_DATE_ISO>"
  }'
```

**Step 2: Check Azure Functions console**
Look for:
- `Teams activity notification sent to <userId>: bookingConfirmed` (SUCCESS -- was previously 403)
- `Teams activity notification sent to <managerId>: managerBookingAlert` (SUCCESS -- was previously 403)
- NO 403 Forbidden errors on sendActivityNotification calls

**Step 3: Verify VRFY-04 (employee Teams notification)**
1. Open Microsoft Teams
2. Click the Activity bell icon (top area)
3. Look for a notification from "RentAVehicle" about the booking
4. Verify:
   - [ ] VRFY-04a: Notification appears in activity feed (not 403 anymore)
   - [ ] VRFY-04b: Preview text includes vehicle name and booking time

**Step 4: Verify VRFY-05 (manager Teams notification)**
Since test user is self-manager, both notifications appear in same activity feed:
1. Look for a second notification with "managerBookingAlert" type
2. Verify:
   - [ ] VRFY-05a: Manager notification appears in activity feed
   - [ ] VRFY-05b: Notification includes employee name and vehicle details

**Report back:**
- Console shows "Teams activity notification sent" (not 403)? (yes/no)
- Employee notification visible in Teams activity feed? (yes/no + describe)
- Manager notification visible in Teams activity feed? (yes/no + describe)
- Any errors? (paste if any)
  </how-to-verify>
  <resume-signal>
Report results for VRFY-04 and VRFY-05:
1. VRFY-04a (employee notification appears): pass/fail
2. VRFY-04b (preview text correct): pass/fail
3. VRFY-05a (manager notification appears): pass/fail
4. VRFY-05b (manager preview correct): pass/fail
5. Console errors: none / paste

Or describe issues for debugging.
  </resume-signal>
  <verify>
    <automated>grep -c "\[x\].*VRFY-04\|VRFY-04.*\[x\]" .planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md || echo "0"</automated>
  </verify>
  <done>
Teams activity feed notifications deliver successfully on the live tenant:
- VRFY-04a: Employee receives bookingConfirmed notification in Teams activity feed
- VRFY-04b: Notification preview includes vehicle name and booking time
- VRFY-05a: Manager receives managerBookingAlert notification in Teams activity feed
- VRFY-05b: Manager notification includes employee name and vehicle details
Verification checklist VRFY-04 and VRFY-05 updated from [~] partial to [x] full pass.
  </done>
</task>

</tasks>

<verification>
- Teams app manifest exists at `spfx/teams/manifest.json` with 5 activityTypes
- activityTypes in manifest exactly match strings in `notificationService.ts`
- SPFx package deployed to App Catalog and synced to Teams
- App installed for test user and visible in Teams
- Creating a booking produces Teams activity feed notifications (no more 403)
- Verification checklist VRFY-04 and VRFY-05 marked full pass
</verification>

<success_criteria>
- Teams app manifest with activityTypes committed to repo
- SPFx package deployed with manifest and installed in Teams for test user
- Employee receives bookingConfirmed notification in Teams activity feed after booking creation
- Manager receives managerBookingAlert notification in Teams activity feed after booking creation
- Azure Functions console shows successful notification delivery (no 403 errors)
- Verification checklist updated: VRFY-04 and VRFY-05 changed from partial [~] to full pass [x]
</success_criteria>

<output>
After completion, create `.planning/phases/09-live-tenant-verification/09-04-SUMMARY.md`
</output>
