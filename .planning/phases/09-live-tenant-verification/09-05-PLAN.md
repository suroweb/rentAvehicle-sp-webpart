---
phase: 09-live-tenant-verification
plan: 05
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - api/local.settings.json
  - .planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md
autonomous: false
gap_closure: true
requirements: [VRFY-03, VRFY-04, VRFY-05]
must_haves:
  truths:
    - "APP_BASE_URL is set to the real SharePoint site URL in local.settings.json"
    - "Email deep links ('View Booking' and 'Cancel Booking' buttons) open the correct SPFx page"
    - "Calendar event body contains a functional 'View in RentAVehicle' link"
    - "No real secrets (AZURE_CLIENT_SECRET, tenant credentials) exist in git history"
  artifacts:
    - path: "api/local.settings.json"
      provides: "APP_BASE_URL set to real SharePoint URL"
      contains: "APP_BASE_URL"
    - path: ".planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md"
      provides: "Updated verification results confirming deep links are functional"
  key_links:
    - from: "api/local.settings.json (APP_BASE_URL)"
      to: "api/src/templates/emailConfirmation.ts"
      via: "process.env.APP_BASE_URL read in notificationService.ts line 78, passed to buildConfirmationEmailHtml"
      pattern: "APP_BASE_URL"
    - from: "api/local.settings.json (APP_BASE_URL)"
      to: "api/src/services/calendarService.ts"
      via: "process.env.APP_BASE_URL read at line 260, passed to buildVehicleEventBody and buildEmployeeEventBody"
      pattern: "APP_BASE_URL"
    - from: "api/local.settings.json (APP_BASE_URL)"
      to: "api/src/services/notificationService.ts"
      via: "process.env.APP_BASE_URL read at line 172 for Teams webUrl construction"
      pattern: "APP_BASE_URL"
---

<objective>
Set APP_BASE_URL to the real SharePoint site URL, re-verify that email and calendar deep links are functional, and confirm that no real secrets were ever committed to git history.

Purpose: APP_BASE_URL is currently empty, causing email "View Booking"/"Cancel Booking" buttons, calendar event "View in RentAVehicle" links, and Teams notification deep links to render as broken bare query strings. Setting this value completes the notification verification chain. The security finding about potential secrets in git history also needs resolution. This plan closes Gap 2 and the additional security finding from the verification report.

Output: Functional deep links in emails, calendar events, and Teams notifications. Security verification documented.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-live-tenant-verification/09-VERIFICATION.md

Key source files:
@api/local.settings.json
@api/local.settings.template.json
@api/src/templates/emailConfirmation.ts
@api/src/services/calendarService.ts
@api/src/services/notificationService.ts
@.gitignore

<interfaces>
From api/src/services/notificationService.ts (lines 78, 118):
```typescript
const appBaseUrl = process.env.APP_BASE_URL || '';
// ...passed to:
buildConfirmationEmailHtml(booking, vehicle, appBaseUrl)
```

From api/src/templates/emailConfirmation.ts (lines 64-65):
```typescript
const viewUrl = `${escapeHtml(appBaseUrl)}?bookingId=${booking.id}`;
const cancelUrl = `${escapeHtml(appBaseUrl)}?bookingId=${booking.id}&amp;action=cancel`;
```

From api/src/services/calendarService.ts (lines 83, 118, 260):
```typescript
const appBaseUrl = process.env.APP_BASE_URL || '';
// Used in event body HTML:
`<a href="${escapeHtml(appBaseUrl)}?bookingId=${booking.id}">View in RentAVehicle</a>`
```

From api/src/services/notificationService.ts (lines 172-188):
```typescript
const appBaseUrl = process.env.APP_BASE_URL || '';
// If SharePoint URL, converts to Teams deep link:
if (appBaseUrl.includes('.sharepoint.com')) {
  const spPageUrl = `${appBaseUrl}?bookingId=${bookingId}`;
  webUrl = `https://teams.microsoft.com/l/entity/com.microsoft.teamspace.tab.web/null?webUrl=${encodeURIComponent(spPageUrl)}`;
}
```

Git history context:
- `api/local.settings.json` was committed in early dev (commits 953c58d through e52b9b3) with EMPTY credential values
- Commit ab4f366 renamed it to template and gitignored the actual file
- Real credentials were added to local.settings.json AFTER it was gitignored
- Conclusion: No real secrets in git history -- only empty placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify git history contains no real secrets and document finding</name>
  <files>.planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md</files>
  <action>
Verify that no real secrets (AZURE_CLIENT_SECRET, AZURE_TENANT_ID, AZURE_CLIENT_ID with actual values) were ever committed to git history. The verification report flagged this as a security concern.

**Investigation steps:**

1. Check all historical versions of `api/local.settings.json` in git:
```bash
git log --all --oneline -- api/local.settings.json
```

2. For each commit that touched the file, inspect the AZURE_CLIENT_SECRET value:
```bash
git show <commit>:api/local.settings.json | grep AZURE_CLIENT_SECRET
```

3. Expected finding: All committed versions should show `"AZURE_CLIENT_SECRET": ""` (empty string). Real credentials were added to the file AFTER commit `ab4f366` which gitignored it.

4. Also verify `api/local.settings.template.json` contains only empty placeholders (no secrets):
```bash
cat api/local.settings.template.json | grep -E "AZURE_CLIENT_SECRET|AZURE_TENANT_ID|AZURE_CLIENT_ID"
```

**Document findings** in the verification checklist Section 8 (Issues Found & Fixes Applied):
- Add a row: "Security verification: local.settings.json git history" / "local.settings.json was committed with empty credential placeholders in early development (commits 953c58d-e52b9b3). Commit ab4f366 gitignored the file and introduced local.settings.template.json. Real credentials were added AFTER gitignore -- no secret rotation needed." / "No fix needed -- verified safe" / "None"

If real secrets ARE found in git history (unexpected):
- Document which commits contain them
- Note that the client secret should be rotated in Entra ID
- Flag for user attention in the resume signal
  </action>
  <verify>
    <automated>git show e52b9b3:api/local.settings.json 2>/dev/null | grep "AZURE_CLIENT_SECRET" | grep -c '""'</automated>
  </verify>
  <done>Git history verified: no real secrets were ever committed. All historical versions of local.settings.json contain empty credential placeholders. The security concern from the verification report is resolved -- no secret rotation needed. Finding documented in verification checklist.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set APP_BASE_URL to the real SharePoint site URL</name>
  <files>api/local.settings.json</files>
  <action>
User must update APP_BASE_URL in the local (gitignored) local.settings.json with the real SharePoint site URL where the SPFx webpart is deployed. Claude cannot determine this URL -- it depends on the tenant's SharePoint site configuration.
  </action>
  <what-built>
Claude verified git history is clean of secrets. The APP_BASE_URL is currently set to `""` (empty string) in `api/local.settings.json`. This causes all deep links in emails, calendar events, and Teams notifications to render as broken bare query strings like `?bookingId=1016` instead of full URLs.

Now the user needs to set the correct SharePoint URL.
  </what-built>
  <how-to-verify>
**Step 1: Determine your SharePoint site URL**
The URL is where the RentAVehicle SPFx webpart is (or will be) deployed. Typically:
```
https://contoso.sharepoint.com/sites/rentavehicle
```
Or if using a different site:
```
https://contoso.sharepoint.com/sites/<your-site-name>
```

If you haven't created a SharePoint site yet, create one:
1. Go to SharePoint admin center (`https://contoso-admin.sharepoint.com`)
2. Create a new team site or communication site named "RentAVehicle"
3. Note the URL

**Step 2: Update local.settings.json**
Edit `api/local.settings.json` and set APP_BASE_URL:
```json
"APP_BASE_URL": "https://contoso.sharepoint.com/sites/rentavehicle"
```

**Important:** The URL should NOT have a trailing slash. It should point to the site root (not a specific page). The code appends `?bookingId=...` query parameters directly.

**Step 3: Restart the API**
```bash
cd api && npm start
```

Report back:
- What URL did you set for APP_BASE_URL?
- API restarted successfully? (yes/no)
  </how-to-verify>
  <resume-signal>
Report:
1. APP_BASE_URL value set to: ___
2. API restarted: yes/no
3. Issues: none / describe
  </resume-signal>
  <verify>
    <automated>grep "APP_BASE_URL" api/local.settings.json | grep -v '""' | grep -c "sharepoint.com\|teams.microsoft.com"</automated>
  </verify>
  <done>APP_BASE_URL is set to the real SharePoint site URL. API restarted with the new value.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create a booking and verify email and calendar deep links are functional</name>
  <files>.planning/phases/09-live-tenant-verification/09-VERIFICATION-CHECKLIST.md</files>
  <action>
After APP_BASE_URL is configured, create a booking and verify that email and calendar event deep links now render correctly with the full SharePoint URL instead of bare query strings.
  </action>
  <what-built>
APP_BASE_URL is now set to the real SharePoint site URL. The code in emailConfirmation.ts, calendarService.ts, and notificationService.ts all read `process.env.APP_BASE_URL` to construct deep links. With the empty value replaced, all links should now be fully qualified URLs.

Expected link format after fix:
- Email "View Booking": `https://contoso.sharepoint.com/sites/rentavehicle?bookingId=1234`
- Email "Cancel Booking": `https://contoso.sharepoint.com/sites/rentavehicle?bookingId=1234&action=cancel`
- Calendar "View in RentAVehicle": `https://contoso.sharepoint.com/sites/rentavehicle?bookingId=1234`
- Teams webUrl: `https://teams.microsoft.com/l/entity/com.microsoft.teamspace.tab.web/null?webUrl=<encoded SP URL>`
  </what-built>
  <how-to-verify>
**Prerequisites:** API running with APP_BASE_URL set.

**Step 1: Create a new booking**
```bash
curl -X POST http://localhost:7071/api/bookings \
  -H "Content-Type: application/json" \
  -d '{
    "vehicleId": <VEHICLE_ID>,
    "startTime": "<FUTURE_DATE_ISO>",
    "endTime": "<FUTURE_DATE_ISO>"
  }'
```

**Step 2: Verify email deep links (VRFY-03 re-verification)**
1. Open Outlook inbox
2. Find the booking confirmation email
3. Hover over (or inspect) the "View Booking" button:
   - Expected: URL starts with your SharePoint site URL, ends with `?bookingId=<ID>`
   - NOT: a bare `?bookingId=<ID>` with no base URL
4. Hover over the "Cancel Booking" button:
   - Expected: URL starts with your SharePoint site URL, ends with `?bookingId=<ID>&action=cancel`
5. Click "View Booking":
   - Expected: Browser navigates to the SharePoint site (it may show a 404 if the SPFx webpart page doesn't handle the bookingId parameter yet -- that's OK as long as the URL itself is correct)

**Step 3: Verify calendar event deep links (VRFY-01/VRFY-02 enhancement)**
1. Open Outlook calendar
2. Find the booking event on the employee's personal calendar
3. Open the event and look for "View in RentAVehicle" link in the body:
   - Expected: Full URL starting with your SharePoint site URL
   - NOT: a bare `?bookingId=<ID>`
4. Check the resource calendar event body too

**Report back:**
- Email "View Booking" URL correct? (yes/no + paste URL)
- Email "Cancel Booking" URL correct? (yes/no + paste URL)
- Calendar event "View in RentAVehicle" URL correct? (yes/no + paste URL)
- Any broken links? (describe)
  </how-to-verify>
  <resume-signal>
Report results:
1. Email "View Booking" link: pass/fail (paste URL if possible)
2. Email "Cancel Booking" link: pass/fail
3. Calendar "View in RentAVehicle" link: pass/fail
4. All URLs start with SharePoint base URL: yes/no
5. Issues: none / describe
  </resume-signal>
  <verify>
    <automated>grep "APP_BASE_URL" api/local.settings.json | grep -v '""' && echo "PASS: APP_BASE_URL configured"</automated>
  </verify>
  <done>
Deep links are functional:
- Email "View Booking" and "Cancel Booking" buttons contain full SharePoint URLs with bookingId parameter
- Calendar event body "View in RentAVehicle" link contains full SharePoint URL
- Teams webUrl constructs a proper Teams deep link from the SharePoint URL
Verification checklist updated to note deep link verification pass. VRFY-03 confirmed fully satisfied (delivery + link correctness). APP_BASE_URL gap is closed.
  </done>
</task>

</tasks>

<verification>
- `api/local.settings.json` has APP_BASE_URL set to a real SharePoint URL (not empty)
- Git history contains no real secrets (verified programmatically)
- Email confirmation deep links render with full URL prefix
- Calendar event body links render with full URL prefix
- Teams notification webUrl correctly encodes the SharePoint URL into a Teams deep link
- Verification checklist Section 8 documents the security verification result
</verification>

<success_criteria>
- APP_BASE_URL is configured with the real SharePoint site URL
- Creating a booking produces an email with functional "View Booking" and "Cancel Booking" links (full URLs, not bare query strings)
- Calendar event body contains a functional "View in RentAVehicle" link with full URL
- Git history verified clean of real secrets -- no rotation needed
- Verification checklist updated with deep link verification results and security finding resolution
</success_criteria>

<output>
After completion, create `.planning/phases/09-live-tenant-verification/09-05-SUMMARY.md`
</output>
