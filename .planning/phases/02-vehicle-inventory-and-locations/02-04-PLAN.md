---
phase: 02-vehicle-inventory-and-locations
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx
  - spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.module.scss
  - spfx/src/webparts/rentaVehicle/components/VehicleForm/PhotoUpload.tsx
  - spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.tsx
  - spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.module.scss
  - spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx
  - spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.module.scss
  - spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
  - spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx
autonomous: false
requirements:
  - VHCL-01
  - VHCL-02
  - M365-04

must_haves:
  truths:
    - "Admin can fill out a full-page form to add a new vehicle with all required fields and a photo"
    - "Admin can edit an existing vehicle's details using the same form pre-populated with current values"
    - "Admin can upload a single photo for a vehicle"
    - "Admin can create, edit, and deactivate vehicle categories"
    - "Admin can view all synced locations with vehicle counts per location"
    - "SuperAdmin can trigger location sync from Entra ID via a button"
    - "Vehicles with no photo show a placeholder"
  artifacts:
    - path: "spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx"
      provides: "Full-page vehicle add/edit form with validation"
      min_lines: 100
    - path: "spfx/src/webparts/rentaVehicle/components/VehicleForm/PhotoUpload.tsx"
      provides: "Single photo upload component with preview"
      min_lines: 40
    - path: "spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.tsx"
      provides: "Category CRUD interface"
      min_lines: 60
    - path: "spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx"
      provides: "Locations list with vehicle counts and sync button"
      min_lines: 50
  key_links:
    - from: "spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx"
      to: "spfx/src/webparts/rentaVehicle/services/ApiService.ts"
      via: "calls createVehicle or updateVehicle on form submit"
      pattern: "createVehicle|updateVehicle"
    - from: "spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx"
      to: "spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx"
      via: "navigates to VehicleForm for add/edit"
      pattern: "VehicleForm"
    - from: "spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx"
      to: "spfx/src/webparts/rentaVehicle/services/ApiService.ts"
      via: "calls getLocations and syncLocations"
      pattern: "syncLocations"
    - from: "spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx"
      to: "spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx"
      via: "renders LocationList when activeNavKey is 'locations'"
      pattern: "activeNavKey.*locations"
    - from: "spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx"
      to: "spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.tsx"
      via: "renders CategoryManagement when activeNavKey is 'categories'"
      pattern: "activeNavKey.*categories"
---

<objective>
Build the vehicle add/edit form with photo upload, category management UI, and locations list page. Complete the fleet management workflow by wiring form navigation from the fleet table.

Purpose: This plan completes the admin's vehicle management workflow. Admins can now add new vehicles (filling out the full-page form per locked decision), edit existing vehicles, manage the category list, and view all synced locations with vehicle counts. The SuperAdmin can trigger location sync from Entra ID. This is the final plan in Phase 2 and includes a human-verify checkpoint.

Output: VehicleForm page, PhotoUpload component, CategoryManagement page, LocationList page, updated AppShell routing, and FleetManagement form navigation.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vehicle-inventory-and-locations/02-RESEARCH.md
@.planning/phases/02-vehicle-inventory-and-locations/02-03-SUMMARY.md
@spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
@spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx
@spfx/src/webparts/rentaVehicle/services/ApiService.ts
@spfx/src/webparts/rentaVehicle/models/IVehicle.ts
@spfx/src/webparts/rentaVehicle/models/ICategory.ts
@spfx/src/webparts/rentaVehicle/models/ILocation.ts
@spfx/src/webparts/rentaVehicle/models/IUser.ts
@spfx/src/webparts/rentaVehicle/styles/variables.module.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VehicleForm page with photo upload and wire navigation from FleetManagement</name>
  <files>
    spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx
    spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.module.scss
    spfx/src/webparts/rentaVehicle/components/VehicleForm/PhotoUpload.tsx
    spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx
    spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
  </files>
  <action>
    1. Create `PhotoUpload.tsx`:
       - Props: `{ photoUrl: string | null, onChange: (url: string | null) => void }`.
       - Per locked decision: "Single photo upload per vehicle."
       - For Phase 2, implement a simple file-to-URL approach: user selects a file via `<input type="file" accept="image/*" />`, display a preview using `URL.createObjectURL()`. Store the file reference for later upload. For now, since Azure Blob Storage setup adds infrastructure complexity, store the photo as a base64 data URL in the photoUrl field (acceptable for Phase 2 per RESEARCH.md open question 3 -- deferring full Blob Storage). Add a TODO comment referencing the Valet Key pattern for production.
       - Show current photo preview if photoUrl exists. Show a placeholder icon (Fluent UI Icon 'Camera') if no photo.
       - Max file size: 2MB. Show error if exceeded.
       - Button: "Upload Photo" / "Change Photo" depending on state.

    2. Create `VehicleForm.tsx`:
       - Props: `{ apiService: ApiService, vehicle?: IVehicle, categories: ICategory[], locations: ILocation[], onSave: () => void, onCancel: () => void }`.
       - Per locked decision: "Full page form for add/edit (navigates away from fleet table)."
       - Form fields using Fluent UI v8 components:
         a. Make: `TextField` (required, max 100)
         b. Model: `TextField` (required, max 100)
         c. Year: `SpinButton` or `TextField` with number validation (required, 1990 to currentYear+2)
         d. License Plate: `TextField` (required, max 20)
         e. Location: `Dropdown` populated from locations prop (required). Show only active locations.
         f. Category: `Dropdown` populated from categories prop (required). Show only active categories.
         g. Capacity: `SpinButton` (required, 1-50, default 5)
         h. Photo: `PhotoUpload` component
       - If `vehicle` prop is provided, pre-populate all fields (edit mode). Title: "Edit Vehicle". Otherwise title: "Add Vehicle".
       - Client-side validation: all required fields filled, year in range, capacity in range.
       - On submit: call `apiService.createVehicle(input)` for new, or `apiService.updateVehicle(vehicle.id, input)` for edit. Show `Spinner` during save. On success, call `onSave()`. On error, show `MessageBar` with error message.
       - Cancel button calls `onCancel()`.
       - Layout: form fields stacked vertically with labels. Save and Cancel buttons at the bottom. Full width on mobile, max-width 600px on desktop.

    3. Create `VehicleForm.module.scss`:
       - Import variables.module.scss. Form container centered with max-width. Field spacing. Button row alignment.

    4. Update `FleetManagement.tsx`:
       - Add state: `editingVehicle: IVehicle | null`, `showForm: boolean`.
       - "Add Vehicle" button: sets `showForm = true, editingVehicle = null`.
       - Edit row action: sets `showForm = true, editingVehicle = selectedVehicle`.
       - When `showForm` is true, render `VehicleForm` instead of the table. Pass `onSave={() => { setShowForm(false); refreshVehicles(); }}` and `onCancel={() => setShowForm(false)}`.
       - This creates the "navigates away from fleet table" experience per locked decision (within the same SPA view, form replaces table).

    5. Update `AppShell.tsx`:
       - Ensure the 'vehicles' route passes all needed props to FleetManagement (apiService, categories, locations -- or let FleetManagement fetch its own data via the apiService).
       - The FleetManagement component should be self-contained: it receives apiService and fetches vehicles/categories/locations internally.
  </action>
  <verify>
    - `cd spfx && npx heft build --clean` compiles without errors
    - VehicleForm.tsx, VehicleForm.module.scss, and PhotoUpload.tsx all exist
    - `grep "createVehicle\|updateVehicle" spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx` confirms API calls
    - `grep "showForm" spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx` confirms form toggle
    - `grep "PhotoUpload" spfx/src/webparts/rentaVehicle/components/VehicleForm/VehicleForm.tsx` confirms photo integration
  </verify>
  <done>VehicleForm is a full-page form with all required fields (make, model, year, plate, location, category, capacity, photo). Works for both add and edit modes. PhotoUpload supports single image selection with preview. FleetManagement toggles between table view and form view. AppShell routing is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Build CategoryManagement and LocationList pages with AppShell routing</name>
  <files>
    spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.tsx
    spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.module.scss
    spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx
    spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.module.scss
    spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
  </files>
  <action>
    1. Create `CategoryManagement.tsx`:
       - Props: `{ apiService: ApiService }`.
       - Per locked decision: "Admin-defined vehicle categories -- admins can create/edit category list (needs category management UI)."
       - Claude's discretion on approach: use an inline management pattern (list + inline form) rather than a separate page for simplicity.
       - Display a list of active categories using Fluent UI `DetailsList` with columns: Name, Description, Actions (edit/deactivate).
       - "Add Category" button opens an inline form at the top with Name and Description fields.
       - Edit action: switch the row to edit mode (inline editing) or show a small form.
       - Deactivate action: show ConfirmDialog ("This will deactivate the category. Vehicles in this category will remain but the category won't be available for new vehicles."). Call `apiService.deleteCategory(id)`.
       - On add/edit submit: call `apiService.createCategory()` or `apiService.updateCategory()`. Refresh list.
       - Simple, functional UI. Page title: "Vehicle Categories".

    2. Create `CategoryManagement.module.scss`:
       - Import variables.module.scss. Layout with padding, form spacing, table styling consistent with FleetManagement.

    3. Create `LocationList.tsx`:
       - Props: `{ apiService: ApiService, userRole: AppRole }`.
       - Per locked decision: "Dedicated locations list view in admin showing all synced offices with vehicle counts per location."
       - Display locations using Fluent UI `DetailsList` with columns: Location Name, Vehicle Count, Status (Active/Inactive badge), Last Synced.
       - Inactive locations (disappeared from Entra ID) highlighted with a warning icon and "Needs Reassignment" badge if they have vehicles assigned.
       - Per locked decision: "If a location disappears from Entra ID, vehicles assigned there are flagged for reassignment -- admin must move them to another location."
       - "Sync Locations" button: visible only to SuperAdmin (`userRole === 'SuperAdmin'`). Calls `apiService.syncLocations()`. Shows spinner during sync. On success, displays `MessageBar` with sync result ("Added: X, Deactivated: Y, Total: Z"). Refreshes the location list.
       - Per locked decision: "Sync triggers: on app startup + manual 'Sync Locations' button for admins." (Button is the manual trigger; startup sync is handled by lazy-sync pattern in API.)
       - Page title: "Office Locations".

    4. Create `LocationList.module.scss`:
       - Import variables.module.scss. Layout consistent with other admin pages.

    5. Update `AppShell.tsx`:
       - Import CategoryManagement and LocationList components.
       - Add routing cases:
         - 'categories': render `<CategoryManagement apiService={apiService} />`
         - 'locations': render `<LocationList apiService={apiService} userRole={auth.user.role} />`
       - Ensure all admin page routes work consistently.
  </action>
  <verify>
    - `cd spfx && npx heft build --clean` compiles without errors
    - All 4 new files exist: CategoryManagement.tsx, CategoryManagement.module.scss, LocationList.tsx, LocationList.module.scss
    - `grep "CategoryManagement\|LocationList" spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx` confirms routing
    - `grep "syncLocations" spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx` confirms sync button
    - `grep "createCategory" spfx/src/webparts/rentaVehicle/components/CategoryManagement/CategoryManagement.tsx` confirms category CRUD
    - `grep "SuperAdmin" spfx/src/webparts/rentaVehicle/components/LocationList/LocationList.tsx` confirms role check for sync button
  </verify>
  <done>CategoryManagement page allows admins to create, edit, and deactivate vehicle categories with inline editing. LocationList page shows all synced locations with vehicle counts and inactive status. SuperAdmin-only sync button triggers Entra ID location sync. AppShell routes to both pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 2 Fleet Management</name>
  <files>None -- verification only</files>
  <action>
    Human verification of the complete Phase 2 delivery.

    What was built:
    - Database schema (Locations, Categories, Vehicles tables)
    - 12 API endpoints (vehicle CRUD, category CRUD, location list, location sync)
    - SuperAdmin role (4th tier) in API and SPFx
    - Fleet management table with sorting, filtering, status badges, row actions
    - Vehicle add/edit form with photo upload
    - Category management page
    - Location list page with sync button
    - Confirmation dialogs for destructive actions

    How to verify:
    1. Run `cd api && npx tsc --noEmit` -- should compile clean
    2. Run `cd spfx && npx heft build --clean` -- should compile clean
    3. Verify API endpoints: Start Azure Functions locally with `cd api && npm start`
       - Test vehicle CRUD: POST, GET, PUT, DELETE, PATCH /api/backoffice/vehicles
       - Test categories: GET, POST /api/backoffice/categories
       - Test locations: GET /api/backoffice/locations
    4. Open SPFx workbench: `cd spfx && gulp serve`
       - Navigate to "Manage Vehicles" -- fleet table should render (empty initially)
       - Click "Add Vehicle" -- form should appear with all fields
       - Navigate to "Categories" -- category management page should render
       - Navigate to "Locations" -- location list should render
       - Verify status badges render with correct colors in the fleet table
       - Verify filter bar has 4 controls (location, status, category, search)
       - Verify three-dot menu on table rows has Edit, Change Status, Remove
  </action>
  <verify>User types "approved" or describes issues found</verify>
  <done>Phase 2 human verification complete. All fleet management features confirmed working.</done>
</task>

</tasks>

<verification>
1. `cd api && npx tsc --noEmit` -- API compiles clean
2. `cd spfx && npx heft build --clean` -- SPFx compiles clean
3. All 9 files in files_modified exist on disk
4. FleetManagement switches between table view and form view
5. VehicleForm handles both add and edit modes
6. CategoryManagement supports create, edit, and deactivate
7. LocationList shows vehicle counts and sync button (SuperAdmin only)
8. AppShell routes to all 3 new pages (vehicles, categories, locations)
</verification>

<success_criteria>
- Vehicle add/edit form works with all fields and photo upload
- Category management allows creating and editing categories
- Location list shows synced locations with vehicle counts
- SuperAdmin sync button triggers location sync
- All pages accessible via navigation
- Both projects compile clean
- Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-vehicle-inventory-and-locations/02-04-SUMMARY.md`
</output>
