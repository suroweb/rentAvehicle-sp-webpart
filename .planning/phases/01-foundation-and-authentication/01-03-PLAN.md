---
phase: 01-foundation-and-authentication
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - PLAT-01
  - PLAT-02
  - PLAT-03

must_haves:
  truths:
    - "User opens webpart in SharePoint page and is automatically signed in with their M365 identity"
    - "User opens the same webpart as a Teams tab and is automatically signed in"
    - "User's role (Employee, Manager, Admin) is displayed in the UI based on their Entra ID app role assignment"
    - "An employee-role user who attempts to call an admin API endpoint receives a 403 Forbidden response"
    - "The webpart renders responsively on Teams mobile"
    - "SPFx workbench shows the app shell with sidebar, navigation, and role badge"
  artifacts:
    - path: "spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.ts"
      provides: "Complete webpart entry point passing all props to AppShell"
      contains: "AppShell"
  key_links:
    - from: "spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.ts"
      to: "spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx"
      via: "React.createElement with all required props"
      pattern: "React\\.createElement.*AppShell"
    - from: "spfx/src/webparts/rentaVehicle/contexts/AuthContext.tsx"
      to: "spfx/src/webparts/rentaVehicle/services/ApiService.ts"
      via: "ApiService.getMe() fetches role from backend"
      pattern: "getMe"
---

<objective>
Verify the complete Phase 1 delivery end-to-end: SPFx app shell rendering in the local workbench, Azure Functions API responding to auth requests, responsive layout switching at 768px breakpoint, and role-based navigation filtering.

Purpose: Ensure all pieces integrate before moving to Phase 2. Catch token audience mismatches, CORS issues, or rendering problems in the local development environment.

Output: Verified working foundation ready for Phase 2 feature development.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-authentication/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build issues and prepare for local testing</name>
  <files>
    spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.ts
  </files>
  <action>
1. Run `cd spfx && npx heft build` and fix any TypeScript or build errors from Plan 02 integration
2. Run `cd api && npm run build` and fix any TypeScript errors
3. Start Azure Functions locally: `cd api && npm start` -- verify all 3 routes are listed (health, me, admin/health)
4. Test API endpoints locally:
   - `curl http://localhost:7071/api/health` -- expect 200 with healthy status
   - `curl http://localhost:7071/api/me` -- expect user context from local dev mock
   - Change `LOCAL_DEV_ROLE` to "Employee" in local.settings.json, restart, call `curl http://localhost:7071/api/admin/health` -- expect 403
   - Change `LOCAL_DEV_ROLE` back to "Admin", restart, call same -- expect 200
5. Start SPFx local workbench: `cd spfx && npx heft start` (or `gulp serve` if Heft start is not available for workbench)
6. The workbench will NOT have real AadHttpClient (it runs outside SharePoint), so AuthContext will fall into the error/fallback path. This is expected behavior. Verify:
   - The ErrorBoundary shows an inline error message with "Try Again" button
   - OR the fallback path renders the app shell with Employee role (if API is unreachable but fallback works)
7. If any component rendering issues, fix them now. Focus on:
   - Import paths correct (relative imports within the webpart)
   - Fluent UI component imports working (`@fluentui/react` not `@fluentui/react-components`)
   - SCSS module imports resolving
   - React 17 compatibility (no React 18 APIs like `useId`, `useSyncExternalStore`)

If the workbench serves successfully and the app shell structure is visible (even with fallback/error state), the local development environment is confirmed working.
  </action>
  <verify>
1. `cd spfx && npx heft build` -- exits 0
2. `cd api && npm run build` -- exits 0
3. Azure Functions starts and lists all routes
4. curl tests pass for all 3 endpoints
5. SPFx workbench serves at https://localhost:4321/temp/workbench.html (or similar)
6. Browser shows the webpart rendering (even in fallback/error state)
  </verify>
  <done>
Both projects build clean. API endpoints respond correctly with role enforcement. SPFx workbench serves the webpart. Any integration issues from Plan 01 + Plan 02 are resolved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 delivery</name>
  <files>
    spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.ts
    api/src/middleware/auth.ts
    spfx/config/package-solution.json
    spfx/src/webparts/rentaVehicle/RentaVehicleWebPart.manifest.json
  </files>
  <action>
Human verification checkpoint. All automated work is complete from Tasks 1. Present the verification steps below to the user and wait for approval.

What was built:
- SPFx webpart configured for SharePoint + Teams hosting
- Azure Functions API with auth middleware (role extraction from x-ms-client-principal)
- Three API endpoints: health (public), me (authenticated), admin/health (admin-only)
- App shell with sidebar navigation (desktop) and bottom tab bar (mobile, below 768px)
- Role-based navigation filtering (admin items hidden from non-admins)
- Welcome/loading screen with user name
- Error boundary with retry button and configurable IT support contact
- AuthContext providing user identity and role to all components

Verification steps for the user:

**API Verification (terminal):**
1. Start API: `cd api && npm start`
2. Test public health: `curl http://localhost:7071/api/health` -- should return `{"status":"healthy","timestamp":"..."}`
3. Test authenticated me: `curl http://localhost:7071/api/me` -- should return user context with role
4. Test admin endpoint with Admin role: `curl http://localhost:7071/api/admin/health` -- should return 200
5. Edit `api/local.settings.json`, change `LOCAL_DEV_ROLE` to `"Employee"`, restart API
6. Test admin endpoint with Employee role: `curl http://localhost:7071/api/admin/health` -- should return 403

**SPFx Verification (browser):**
7. Start workbench: `cd spfx && npx heft start` (open https://localhost:4321/temp/workbench.html)
8. Add the RentAVehicle webpart to the workbench
9. Verify: App renders (likely in error/fallback state since workbench has no real auth)
10. Verify: If in error state, inline error message appears with "Try Again" button
11. Resize browser window below 768px wide -- verify layout switches (sidebar disappears, bottom bar appears or layout adjusts)
12. Resize back above 768px -- verify sidebar returns

**Code Review (optional):**
13. Open `spfx/config/package-solution.json` -- verify `webApiPermissionRequests` with `user_impersonation`
14. Open webpart manifest -- verify `supportedHosts` includes `TeamsTab`
15. Open `api/src/middleware/auth.ts` -- verify role hierarchy logic (Admin > Manager > Employee)
  </action>
  <verify>User confirms all verification steps pass by typing "approved"</verify>
  <done>Human has verified: API endpoints work with correct role enforcement, SPFx webpart renders in workbench, responsive layout switches correctly, and code review confirms proper configuration.</done>
</task>

</tasks>

<verification>
1. Both SPFx and Azure Functions projects build without errors
2. API endpoints enforce role-based access correctly (403 for unauthorized roles)
3. SPFx webpart renders in local workbench
4. Responsive layout switches at 768px breakpoint
5. All Phase 1 success criteria from ROADMAP are addressed in the codebase
</verification>

<success_criteria>
- SPFx project builds and serves in local workbench
- Azure Functions project builds and starts with all 3 endpoints
- Role enforcement: Employee gets 403 on admin endpoints, Admin gets 200
- Webpart manifest declares SharePoint + Teams hosts
- Package-solution declares API permissions
- App shell renders with sidebar (desktop) and bottom tab bar (mobile)
- Error handling shows inline message with retry
- Human verifies the above and approves
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-03-SUMMARY.md`
</output>
