---
phase: 02-vehicle-inventory-and-locations
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - spfx/src/webparts/rentaVehicle/models/IVehicle.ts
  - spfx/src/webparts/rentaVehicle/models/ICategory.ts
  - spfx/src/webparts/rentaVehicle/models/ILocation.ts
  - spfx/src/webparts/rentaVehicle/services/ApiService.ts
  - spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
  - spfx/src/webparts/rentaVehicle/components/Sidebar/navItems.ts
  - spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx
  - spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.module.scss
  - spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx
  - spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleFilterBar.tsx
  - spfx/src/webparts/rentaVehicle/components/FleetManagement/StatusBadge.tsx
  - spfx/src/webparts/rentaVehicle/components/ConfirmDialog/ConfirmDialog.tsx
autonomous: true
requirements:
  - VHCL-01
  - VHCL-02
  - VHCL-03
  - VHCL-04

must_haves:
  truths:
    - "Admin navigates to 'Manage Vehicles' and sees a data table of vehicles at their location"
    - "Admin can sort table columns by clicking column headers"
    - "Admin can filter vehicles by location, status, category, and free-text search"
    - "Admin can click three-dot menu on a row to access Edit, Change Status, and Remove actions"
    - "Status badges show colored pills: green=Available, yellow=Maintenance, red=Retired, blue=Reserved"
    - "Confirmation dialog appears before status change or removal with explanation of impact"
    - "Vehicle table shows thumbnail image in first column"
  artifacts:
    - path: "spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx"
      provides: "Fleet management page orchestrating table, filters, and actions"
      min_lines: 50
    - path: "spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx"
      provides: "Fluent UI DetailsList with columns, sorting, thumbnails, status badges, row actions"
      min_lines: 80
    - path: "spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleFilterBar.tsx"
      provides: "Filter bar with location, status, category dropdowns and search box"
      min_lines: 40
    - path: "spfx/src/webparts/rentaVehicle/components/FleetManagement/StatusBadge.tsx"
      provides: "Colored status pill component"
      min_lines: 20
    - path: "spfx/src/webparts/rentaVehicle/components/ConfirmDialog/ConfirmDialog.tsx"
      provides: "Reusable confirmation dialog using Fluent UI Dialog"
      min_lines: 30
    - path: "spfx/src/webparts/rentaVehicle/services/ApiService.ts"
      provides: "Extended ApiService with vehicle, category, and location methods"
      contains: "getVehicles"
    - path: "spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx"
      provides: "Updated with page routing based on activeNavKey"
      contains: "FleetManagement"
  key_links:
    - from: "spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx"
      to: "spfx/src/webparts/rentaVehicle/services/ApiService.ts"
      via: "calls apiService.getVehicles() to load fleet data"
      pattern: "getVehicles"
    - from: "spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx"
      to: "spfx/src/webparts/rentaVehicle/components/FleetManagement/StatusBadge.tsx"
      via: "renders StatusBadge in status column"
      pattern: "StatusBadge"
    - from: "spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx"
      to: "spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx"
      via: "renders FleetManagement when activeNavKey is 'vehicles'"
      pattern: "activeNavKey.*vehicles"
    - from: "spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx"
      to: "spfx/src/webparts/rentaVehicle/components/ConfirmDialog/ConfirmDialog.tsx"
      via: "shows confirmation before status change or archive"
      pattern: "ConfirmDialog"
---

<objective>
Build the fleet management data table UI with filtering, sorting, status badges, row actions, and confirmation dialogs.

Purpose: This delivers the primary admin experience for viewing and managing the vehicle fleet. The data table with filter bar is the locked design decision from the user -- rows with columns, sortable and filterable, with thumbnail, status badges, and three-dot row action menus. This plan also establishes the page routing pattern in AppShell and extends ApiService with vehicle/category/location API methods.

Output: FleetManagement page with VehicleTable (DetailsList), VehicleFilterBar, StatusBadge, ConfirmDialog, extended ApiService, and AppShell routing.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vehicle-inventory-and-locations/02-RESEARCH.md
@.planning/phases/02-vehicle-inventory-and-locations/02-01-SUMMARY.md
@.planning/phases/02-vehicle-inventory-and-locations/02-02-SUMMARY.md
@spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
@spfx/src/webparts/rentaVehicle/components/Sidebar/navItems.ts
@spfx/src/webparts/rentaVehicle/services/ApiService.ts
@spfx/src/webparts/rentaVehicle/models/IUser.ts
@spfx/src/webparts/rentaVehicle/styles/variables.module.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SPFx interfaces, extend ApiService, and add page routing to AppShell</name>
  <files>
    spfx/src/webparts/rentaVehicle/models/IVehicle.ts
    spfx/src/webparts/rentaVehicle/models/ICategory.ts
    spfx/src/webparts/rentaVehicle/models/ILocation.ts
    spfx/src/webparts/rentaVehicle/services/ApiService.ts
    spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx
    spfx/src/webparts/rentaVehicle/components/Sidebar/navItems.ts
  </files>
  <action>
    1. Create `spfx/src/webparts/rentaVehicle/models/IVehicle.ts`:
       - `IVehicle` interface matching the API response: id, make, model, year, licensePlate, locationId, locationName, categoryId, categoryName, capacity, photoUrl (string|null), status ('Available'|'InMaintenance'|'Retired'|'Reserved'), isArchived, createdAt, updatedAt.
       - `IVehicleInput` interface: make, model, year, licensePlate, locationId, categoryId, capacity, photoUrl (string|null|undefined).
       - `IVehicleFilters` interface: locationId?, status?, categoryId?, search? (all optional).
       - `VehicleStatus` type: 'Available' | 'InMaintenance' | 'Retired' | 'Reserved'.

    2. Create `spfx/src/webparts/rentaVehicle/models/ICategory.ts`:
       - `ICategory` interface: id, name, description (string|null), isActive, createdAt, updatedAt.
       - `ICategoryInput` interface: name, description?.

    3. Create `spfx/src/webparts/rentaVehicle/models/ILocation.ts`:
       - `ILocation` interface: id, name, isActive, vehicleCount?, lastSyncedAt, createdAt, updatedAt.
       - `ILocationSyncResult` interface: added, deactivated, total.

    4. Extend `spfx/src/webparts/rentaVehicle/services/ApiService.ts`:
       - Add private helper methods: `post<T>(path, body)`, `put<T>(path, body)`, `patch<T>(path, body)`, `delete<T>(path)` alongside existing `get<T>(path)`. All use this.client with AadHttpClient.configurations.v1.
       - Add vehicle methods: `getVehicles(filters?: IVehicleFilters)` (builds query string from filters), `getVehicleById(id: number)`, `createVehicle(input: IVehicleInput)`, `updateVehicle(id: number, input: IVehicleInput)`, `deleteVehicle(id: number)` (soft delete), `updateVehicleStatus(id: number, status: string)`.
       - Add category methods: `getCategories()`, `createCategory(input: ICategoryInput)`, `updateCategory(id: number, input: ICategoryInput)`, `deleteCategory(id: number)`.
       - Add location methods: `getLocations()`, `syncLocations()`.
       - All API paths use `/api/backoffice/` prefix.

    5. Update `spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx`:
       - Import FleetManagement component (lazy import or direct).
       - Add a `renderPage(activeNavKey: string)` function that switches on activeNavKey:
         - 'home': render the existing welcome content (Welcome heading + text)
         - 'vehicles': render `<FleetManagement apiService={apiService} />` where apiService is constructed from the AadHttpClient. The apiService needs to be passed through -- either via context or props. Use a simple approach: create the ApiService instance in AppShellContent and pass it to page components.
         - All other keys: render a placeholder "Coming soon" message.
       - Extract the welcome content into the renderPage function to keep AppShellContent clean.
       - Pass apiService as a prop to AppShellContent from AppShell (construct it from apiClient prop). OR create it inside AppShellContent using the apiClient from AuthContext. Check how apiClient is currently available -- it's passed as a prop to AppShell. Thread it through.

    6. Update `spfx/src/webparts/rentaVehicle/components/Sidebar/navItems.ts`:
       - Add a new nav item for Locations: `{ key: 'locations', label: 'Locations', icon: 'MapPin', minRole: 'Admin' }` -- insert after 'vehicles' item.
       - Add a nav item for Categories: `{ key: 'categories', label: 'Categories', icon: 'Tag', minRole: 'Admin' }` -- insert after 'locations' item.
       - This gives admin 3 management items: Manage Vehicles, Locations, Categories.
  </action>
  <verify>
    - `cd spfx && npx heft build --clean` compiles without errors
    - `grep "getVehicles" spfx/src/webparts/rentaVehicle/services/ApiService.ts` confirms vehicle methods
    - `grep "FleetManagement" spfx/src/webparts/rentaVehicle/components/AppShell/AppShell.tsx` confirms routing
    - `grep "locations" spfx/src/webparts/rentaVehicle/components/Sidebar/navItems.ts` confirms new nav item
    - All 3 model files exist: IVehicle.ts, ICategory.ts, ILocation.ts
  </verify>
  <done>SPFx interfaces mirror API models. ApiService has full CRUD methods for vehicles, categories, and locations. AppShell routes to FleetManagement when 'vehicles' nav key is active. Navigation includes Locations and Categories items for admins.</done>
</task>

<task type="auto">
  <name>Task 2: Build FleetManagement page with VehicleTable, filters, StatusBadge, and ConfirmDialog</name>
  <files>
    spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx
    spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.module.scss
    spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx
    spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleFilterBar.tsx
    spfx/src/webparts/rentaVehicle/components/FleetManagement/StatusBadge.tsx
    spfx/src/webparts/rentaVehicle/components/ConfirmDialog/ConfirmDialog.tsx
  </files>
  <action>
    1. Create `StatusBadge.tsx` (from RESEARCH.md StatusBadge example):
       - Colored pill component. Props: `{ status: string }`.
       - Colors per locked decision: green=Available (#DFF6DD bg, #107C10 text), yellow=InMaintenance (#FFF4CE bg, #797600 text), red=Retired (#FDE7E9bg, #D13438 text), blue=Reserved (#DEECF9 bg, #0078D4 text).
       - Display labels: 'Available', 'In Maintenance', 'Retired', 'Reserved' (human-friendly, not enum values).
       - Inline styles for the pill (borderRadius: 12, padding: 2px 8px, fontSize: 12, fontWeight: 600).

    2. Create `ConfirmDialog.tsx`:
       - Reusable confirmation dialog using Fluent UI v8 `Dialog`, `DialogType`, `DialogFooter`, `PrimaryButton`, `DefaultButton`.
       - Props: `{ hidden: boolean, title: string, message: string, confirmLabel: string, onConfirm: () => void, onDismiss: () => void }`.
       - DialogType.normal. Show title and message. Confirm button (danger style for destructive actions) and Cancel button.
       - Per locked decision: "All status changes require confirmation dialog explaining what will happen."

    3. Create `VehicleFilterBar.tsx`:
       - Props: `{ locations: ILocation[], categories: ICategory[], filters: IVehicleFilters, onFilterChange: (filters: IVehicleFilters) => void }`.
       - Use Fluent UI `Dropdown` for location (all locations + "All Locations" option), status dropdown (Available, In Maintenance, Retired, Reserved + "All Statuses"), category dropdown (all categories + "All Categories").
       - Use Fluent UI `SearchBox` for free-text search across make/model/plate.
       - Per locked decision: "Full filter bar: location, status, category, and free-text search across make/model/plate."
       - Layout: horizontal row of filter controls. Wrap on mobile.
       - Debounce search box input (300ms) before calling onFilterChange.

    4. Create `VehicleTable.tsx`:
       - Props: `{ vehicles: IVehicle[], columns: IColumn[], onSort: (columnKey: string, descending: boolean) => void, onEdit: (vehicle: IVehicle) => void, onChangeStatus: (vehicle: IVehicle) => void, onRemove: (vehicle: IVehicle) => void }`.
       - Use Fluent UI v8 `DetailsList` with `DetailsListLayoutMode.justified` and `SelectionMode.none`.
       - Columns per locked decision (discretion: column order):
         a. Photo thumbnail (40px, no header, renders `<img>` 32x32 rounded, fallback to a generic car icon if photoUrl is null -- use Fluent UI `Icon` with 'Car' icon name as placeholder).
         b. Make (sortable)
         c. Model (sortable)
         d. Year (sortable)
         e. License Plate
         f. Location (locationName)
         g. Category (categoryName)
         h. Capacity
         i. Status (renders StatusBadge)
         j. Actions (40px, renders IconButton with MoreVertical icon and context menu)
       - Row actions menu (per locked decision: "Row actions menu (three-dot) for Edit, Change Status, Remove"):
         - Edit: calls onEdit
         - Change Status: calls onChangeStatus
         - Remove: calls onRemove
       - Sorting: `onColumnClick` handler. Track sortColumn and sortDescending in local state. Sort items array using `React.useMemo()` for performance (per RESEARCH.md Pitfall 4).
       - Use `React.useCallback()` for the column click handler.

    5. Create `FleetManagement.tsx`:
       - Props: `{ apiService: ApiService }` (or access via context).
       - State: vehicles (IVehicle[]), categories (ICategory[]), locations (ILocation[]), filters (IVehicleFilters), loading (boolean), error (string|null).
       - State for dialogs: confirmDialog { hidden, title, message, confirmLabel, onConfirm }.
       - On mount: fetch vehicles, categories, and locations from API via apiService.
       - When filters change: re-fetch vehicles with updated filters.
       - "Add Vehicle" button at top (navigates to vehicle form -- for now, placeholder that sets a state to show form page; actual form is in Plan 04).
       - Handle row actions:
         - Edit: set state to navigate to edit form (placeholder for Plan 04)
         - Change Status: show ConfirmDialog with status options. On confirm, call apiService.updateVehicleStatus(). Refresh list. Confirmation message per locked decision: explain impact (e.g., "Changing to 'In Maintenance' will hide this vehicle from employee browsing").
         - Remove: show ConfirmDialog ("This will archive the vehicle. It will no longer appear in the fleet list. This action can be reversed."). On confirm, call apiService.deleteVehicle(). Refresh list.
       - Render: page title "Fleet Management", VehicleFilterBar, VehicleTable, ConfirmDialog.
       - Show Fluent UI `Spinner` during loading, `MessageBar` on error.

    6. Create `FleetManagement.module.scss`:
       - Import variables.module.scss for consistent styling.
       - Page layout with padding, filter bar spacing, table container.
       - Header row with title and "Add Vehicle" button.
  </action>
  <verify>
    - `cd spfx && npx heft build --clean` compiles without errors
    - All 6 files exist in their expected locations
    - `grep "DetailsList" spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx` confirms Fluent UI table usage
    - `grep "StatusBadge" spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx` confirms badge rendering in table
    - `grep "ConfirmDialog" spfx/src/webparts/rentaVehicle/components/FleetManagement/FleetManagement.tsx` confirms dialog integration
    - `grep "IconButton" spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleTable.tsx` confirms row actions menu
    - `grep "SearchBox" spfx/src/webparts/rentaVehicle/components/FleetManagement/VehicleFilterBar.tsx` confirms search
  </verify>
  <done>FleetManagement page renders a sortable, filterable data table of vehicles with thumbnails, colored status badges, and three-dot row action menus. Filter bar supports location, status, category, and free-text search. Confirmation dialogs appear before status changes and removal. All per locked design decisions.</done>
</task>

</tasks>

<verification>
1. `cd spfx && npx heft build --clean` -- SPFx project compiles without errors
2. All 12 files in files_modified exist on disk
3. Navigate to 'vehicles' in AppShell and FleetManagement page renders
4. VehicleTable uses DetailsList with all 10 columns
5. StatusBadge renders 4 colors for 4 statuses
6. VehicleFilterBar has 3 dropdowns + 1 search box
7. ConfirmDialog is used for status change and removal actions
8. Row actions menu has Edit, Change Status, Remove
</verification>

<success_criteria>
- Fleet management table renders with all columns per locked decision
- Sorting works via column header clicks
- Filtering works via location, status, category, and search
- Row action menus provide Edit, Change Status, Remove
- Confirmation dialogs appear with impact explanation
- Status badges show correct colors
- AppShell routes to FleetManagement page
- SPFx build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-vehicle-inventory-and-locations/02-03-SUMMARY.md`
</output>
